# Nixpacks configuration for Railway
# This file should be used when Root Directory is set to 'backend'

[providers]
node = "18"

[phases.setup]
nixPkgs = ["nodejs-18_x"]

[phases.install]
cmds = ["npm install"]

[start]
cmd = "npm start"

